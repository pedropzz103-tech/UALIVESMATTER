const cfg=window.UA_CONFIG||{};let selectedType='danger',currentChannel='general',sb=null,realtime=false;
const map=L.map('map',{zoomControl:false}).setView([49.0,31.2],6);L.tileLayer('https://tile.openstreetmap.org/{z}/{x}/{y}.png',{maxZoom:19,attribution:'&copy; OpenStreetMap contributors'}).addTo(map);L.control.zoom({position:'bottomleft'}).addTo(map);
const utilityCommunity=L.layerGroup(),utilityOfficial=L.layerGroup();
const layers={community:L.layerGroup().addTo(map),utilities:L.layerGroup([utilityCommunity,utilityOfficial]).addTo(map),shelter:L.layerGroup().addTo(map),transport:L.layerGroup(),official:L.layerGroup()};
function icon(c){return L.divIcon({className:'',html:'<div class="'+c+'"></div>',iconSize:[18,18],iconAnchor:[9,9]})}
const icons={community:icon('community-dot'),power:icon('power-dot'),heating:icon('heating-dot'),water:icon('water-dot'),shelter:icon('shelter-dot'),transport:icon('transport-dot'),official:icon('official-dot')};
function esc(s=''){return String(s).replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#039;'}[m]))}
function page(n,b){document.querySelectorAll('.page').forEach(x=>x.classList.remove('active'));document.getElementById('p-'+n).classList.add('active');document.querySelectorAll('.nav button').forEach(x=>x.classList.remove('active'));b.classList.add('active');if(n==='map')setTimeout(()=>map.invalidateSize(),80)}
function toggleLayer(n,b){if(map.hasLayer(layers[n]))map.removeLayer(layers[n]);else map.addLayer(layers[n]);b.classList.toggle('on');if(n==='shelter'&&map.hasLayer(layers[n]))loadShelters();if(n==='transport'&&map.hasLayer(layers[n]))loadTransport();if(n==='official'&&map.hasLayer(layers[n]))loadOfficialAlerts()}
function openAlert(){document.getElementById('alertSheet').classList.add('open')}function closeAlert(){document.getElementById('alertSheet').classList.remove('open')}function pickType(el,t){selectedType=t;document.querySelectorAll('.type').forEach(x=>x.classList.remove('sel'));el.classList.add('sel')}
function nativeLoc(){try{let r=window.Android?.getLastKnownLocation?.();return r?JSON.parse(r):null}catch(e){return null}}
function locateMe(){const p=nativeLoc();if(p){map.setView([p.lat,p.lng],15);L.circleMarker([p.lat,p.lng],{radius:7,color:'#fff',weight:3,fillColor:'#2f8cff',fillOpacity:1}).addTo(map).bindPopup('Ваша приблизна позиція').openPopup();loadNearbyShelters(p.lat,p.lng)}else alert('Позиція недоступна. Перевірте дозвіл геолокації.')}
function showPosition(){const p=nativeLoc();document.getElementById('myPos').textContent=p?('Широта: '+p.lat.toFixed(5)+' · Довгота: '+p.lng.toFixed(5)):'Позиція недоступна.'}
function dial(n){try{if(window.Android?.callEmergency){Android.callEmergency(n);return}}catch(e){}window.location.href='tel:'+n}
function rounded(v){return Math.round(v*1000)/1000}
async function submitAlert(){const txt=document.getElementById('alertText').value.trim()||'Повідомлення користувача';let p=nativeLoc(),lat,lng;if(p){lat=rounded(p.lat);lng=rounded(p.lng)}else{const c=map.getCenter();lat=rounded(c.lat);lng=rounded(c.lng)}const a={type:selectedType,text:txt,lat,lng,status:'unverified',confirmations:0,rejections:0,created_at:new Date().toISOString()};if(realtime){const {error}=await sb.from('community_alerts').insert(a);if(error){alert('Не вдалося опублікувати.');return}}else{let arr=JSON.parse(localStorage.getItem('alerts')||'[]');a.id='local-'+Date.now();arr.push(a);localStorage.setItem('alerts',JSON.stringify(arr));renderAlert(a)}document.getElementById('alertText').value='';closeAlert()}
function isUtilityType(t){return ['power','heating','water'].includes(t)}
function typeLabel(t){return ({power:'⚡ Відключення електроенергії',heating:'♨️ Відсутнє опалення',water:'💧 Відсутня вода',danger:'⚠️ Небезпека',fire:'🔥 Пожежа',road:'🚧 Перекрита дорога',shelter:'🛡️ Проблема з укриттям',medical:'🩹 Медична допомога'})[t]||t}
function renderAlert(a){const verified=(a.confirmations||0)>=3&&(a.confirmations||0)>(a.rejections||0);const label=verified?'Підтверджено спільнотою':'Неперевірене';const cls=verified?'verified':'unverified';const util=isUtilityType(a.type);const target=util?utilityCommunity:layers.community;const ic=util?(icons[a.type]||icons.community):icons.community;const m=L.marker([a.lat,a.lng],{icon:ic}).addTo(target);m.bindPopup('<b>'+esc(typeLabel(a.type))+'</b><br>'+esc(a.text)+'<br><span class="'+cls+'">'+label+'</span><br><button onclick="voteAlert(\''+esc(a.id)+'\',true)">✓ Підтвердити</button> <button onclick="voteAlert(\''+esc(a.id)+'\',false)">✕ Спростувати</button>')}
async function voteAlert(id,ok){if(!realtime){alert('Голосування між користувачами активується після підключення realtime-сервера.');return}const device=getDeviceId();const {error}=await sb.from('alert_votes').upsert({alert_id:id,device_id:device,vote:ok?1:-1},{onConflict:'alert_id,device_id'});if(error)alert('Не вдалося зберегти голос.')}
function getDeviceId(){let id=localStorage.getItem('deviceId');if(!id){id=crypto.randomUUID?crypto.randomUUID():('dev-'+Date.now()+'-'+Math.random());localStorage.setItem('deviceId',id)}return id}
async function initBackend(){if(!cfg.supabaseUrl||!cfg.supabaseAnonKey){setStatus(false);loadLocalAlerts();renderChat();return}try{sb=window.supabase.createClient(cfg.supabaseUrl,cfg.supabaseAnonKey);const {error}=await sb.from('community_alerts').select('id',{head:true,count:'exact'});if(error)throw error;realtime=true;setStatus(true);await Promise.all([syncAlerts(),syncUtilities()]);subscribeRealtime();await renderChat()}catch(e){console.warn(e);realtime=false;setStatus(false);loadLocalAlerts();renderChat()}}
function setStatus(live){const e=document.getElementById('backendStatus');e.textContent=live?'REALTIME':'OFFLINE';e.classList.toggle('live',live)}
function loadLocalAlerts(){try{JSON.parse(localStorage.getItem('alerts')||'[]').forEach(renderAlert)}catch(e){}}
async function syncAlerts(){layers.community.clearLayers();utilityCommunity.clearLayers();const {data}=await sb.from('community_alerts').select('*').gte('created_at',new Date(Date.now()-24*3600e3).toISOString()).order('created_at',{ascending:false}).limit(300);(data||[]).forEach(renderAlert)}
function subscribeRealtime(){sb.channel('ualives-realtime').on('postgres_changes',{event:'*',schema:'public',table:'community_alerts'},()=>syncAlerts()).on('postgres_changes',{event:'*',schema:'public',table:'alert_votes'},()=>syncAlerts()).on('postgres_changes',{event:'*',schema:'public',table:'utility_incidents'},()=>syncUtilities()).on('postgres_changes',{event:'INSERT',schema:'public',table:'chat_messages'},p=>{if(p.new.channel===currentChannel)appendMessage(p.new)}).subscribe()}
async function switchChannel(ch,b){currentChannel=ch;document.querySelectorAll('[data-channel]').forEach(x=>x.classList.remove('on'));b.classList.add('on');await renderChat()}
async function renderChat(){const box=document.getElementById('messages');box.innerHTML='';if(realtime){const {data}=await sb.from('chat_messages').select('*').eq('channel',currentChannel).order('created_at',{ascending:false}).limit(80);(data||[]).reverse().forEach(appendMessage)}else{let all=JSON.parse(localStorage.getItem('chat')||'[]');all.filter(x=>x.channel===currentChannel).forEach(appendMessage);if(!all.length)appendMessage({display_name:'UA LIVES MATTER',text:'Realtime-чат увімкнеться після підключення сервера.',created_at:new Date().toISOString()})}}
function appendMessage(m){const box=document.getElementById('messages'),d=document.createElement('div');d.className='msg';d.innerHTML='<b>'+esc(m.display_name||'Користувач')+'</b><p>'+esc(m.text||'')+'</p><div class="meta">'+new Date(m.created_at||Date.now()).toLocaleTimeString('uk-UA',{hour:'2-digit',minute:'2-digit'})+'</div>';box.appendChild(d);box.scrollTop=box.scrollHeight}
async function sendChat(){const i=document.getElementById('chatInput'),t=i.value.trim();if(!t)return;const m={channel:currentChannel,display_name:'Користувач',text:t,device_id:getDeviceId(),created_at:new Date().toISOString()};if(realtime){const {error}=await sb.from('chat_messages').insert(m);if(error){alert('Не вдалося надіслати.');return}}else{let all=JSON.parse(localStorage.getItem('chat')||'[]');all.push(m);localStorage.setItem('chat',JSON.stringify(all));appendMessage(m)}i.value=''}
let shelterLoaded=false;async function loadShelters(){if(shelterLoaded)return;shelterLoaded=true;await Promise.allSettled([loadKyivShelters(),loadNearbyShelters(map.getCenter().lat,map.getCenter().lng)])}
async function loadKyivShelters(){try{const r=await fetch(cfg.kyivSheltersUrl);if(!r.ok)throw 0;const g=await r.json();L.geoJSON(g,{pointToLayer:(f,ll)=>L.marker(ll,{icon:icons.shelter}),onEachFeature:(f,l)=>{const p=f.properties||{};l.bindPopup('<b>Офіційне укриття · Київ</b><br>'+esc(p.address||p.Adress||p.name||p.type_building||'')+'<br><small>Джерело: Портал даних Києва</small>')}}).addTo(layers.shelter)}catch(e){console.warn('Kyiv shelters unavailable',e)}}
async function loadNearbyShelters(lat,lng){try{const q='[out:json][timeout:20];(nwr(around:25000,'+lat+','+lng+')[amenity=shelter][shelter_type=bomb_shelter];nwr(around:25000,'+lat+','+lng+')[military=bunker][bunker_type=bomb_shelter];);out center tags;';const r=await fetch(cfg.overpassUrl,{method:'POST',headers:{'Content-Type':'application/x-www-form-urlencoded'},body:'data='+encodeURIComponent(q)});const j=await r.json();(j.elements||[]).forEach(e=>{const la=e.lat??e.center?.lat,lo=e.lon??e.center?.lon;if(!la||!lo)return;L.marker([la,lo],{icon:icons.shelter}).addTo(layers.shelter).bindPopup('<b>Укриття OpenStreetMap</b><br>'+esc(e.tags?.name||e.tags?.['name:uk']||'Bomb shelter')+'<br><small>Перевірте доступність на місці.</small>')})}catch(e){console.warn('OSM shelters unavailable',e)}}
let transportLoaded=false;async function loadTransport(){if(transportLoaded)return;transportLoaded=true;const c=map.getCenter();try{const q='[out:json][timeout:20];(nwr(around:15000,'+c.lat+','+c.lng+')[railway=station];nwr(around:15000,'+c.lat+','+c.lng+')[public_transport=station];nwr(around:15000,'+c.lat+','+c.lng+')[highway=bus_stop];);out center tags;';const r=await fetch(cfg.overpassUrl,{method:'POST',headers:{'Content-Type':'application/x-www-form-urlencoded'},body:'data='+encodeURIComponent(q)});const j=await r.json();(j.elements||[]).slice(0,250).forEach(e=>{const la=e.lat??e.center?.lat,lo=e.lon??e.center?.lon;if(!la||!lo)return;L.marker([la,lo],{icon:icons.transport}).addTo(layers.transport).bindPopup('<b>Транспорт</b><br>'+esc(e.tags?.name||'Зупинка / станція')+'<br><small>OSM: місце, не realtime-статус.</small>')})}catch(e){}}
async function loadOfficialAlerts(){layers.official.clearLayers();if(!cfg.airAlertsProxy){L.marker([50.45,30.52],{icon:icons.official}).addTo(layers.official).bindPopup('<b>Офіційні тривоги</b><br>Потрібне підключення захищеного API-проксі.');return}try{const r=await fetch(cfg.airAlertsProxy);const data=await r.json();(data.states||data||[]).filter(x=>x.active||x.alert).forEach(x=>{if(x.lat&&x.lng)L.marker([x.lat,x.lng],{icon:icons.official}).addTo(layers.official).bindPopup('<b>Офіційна тривога</b><br>'+esc(x.name||x.region||''))})}catch(e){console.warn(e)}}
async function syncUtilities(){utilityOfficial.clearLayers();if(!realtime)return;const {data,error}=await sb.from('utility_incidents').select('*').eq('status','active').order('created_at',{ascending:false}).limit(300);if(error){console.warn(error);return}(data||[]).forEach(renderUtilityIncident)}
function renderUtilityIncident(x){if(!x.lat||!x.lng)return;const ic=icons[x.service]||icons.official;let eta=x.expected_restore_at?('<br><b>Очікуване відновлення:</b> '+new Date(x.expected_restore_at).toLocaleString('uk-UA')):'';let src=x.source_name?('<br><small>Джерело: '+esc(x.source_name)+'</small>'):'';L.marker([x.lat,x.lng],{icon:ic}).addTo(utilityOfficial).bindPopup('<b>'+esc(typeLabel(x.service))+'</b><br>'+esc(x.title||'Комунальна аварія')+(x.details?'<br>'+esc(x.details):'')+eta+src)}
const kitItems=['Вода','Їжа тривалого зберігання','Документи та копії','Ліки','Аптечка','Power bank','Ліхтарик','Радіо / резервний зв’язок','Теплий одяг','Готівка'];function renderKit(){const s=JSON.parse(localStorage.getItem('kit')||'{}');document.getElementById('kit').innerHTML=kitItems.map((x,i)=>'<label class="check"><input type="checkbox" '+(s[i]?'checked':'')+' onchange="saveKit('+i+',this.checked)"> <span>'+x+'</span></label>').join('')}function saveKit(i,v){const s=JSON.parse(localStorage.getItem('kit')||'{}');s[i]=v;localStorage.setItem('kit',JSON.stringify(s))}
renderKit();initBackend();loadShelters();